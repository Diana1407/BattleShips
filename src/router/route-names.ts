export const enum AuthRouteNames {
    LOGIN = 'LOGIN',
    REGISTER = 'REGISTER'
}

export const enum GameRouteNames {
    TABLE = 'TABLE',
    LOBBY = 'LOBBY'
}

export const enum UserRouteNames {
    PROFILE = 'PROFILE'
}